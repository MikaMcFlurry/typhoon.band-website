# =================================================================
# Typhoon — environment template
# Copy to `.env.local` for development. NEVER commit real secrets.
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# All others stay on the server (Vercel: do NOT mark them "Public").
# =================================================================

# --- Public Supabase values (browser + server) -------------------
# Project URL + anon key from Supabase Dashboard → Project Settings → API.
# Both are safe to ship to the browser; the anon key is bound by RLS.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Server-only secrets — never expose in the browser -----------
# Service-role key BYPASSES RLS. Only used by server routes.
# DO NOT prefix with NEXT_PUBLIC_ and do not paste into the client bundle.
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=

# --- Mail --------------------------------------------------------
# Booking submissions are mailed to BOOKING_EMAIL via Resend.
# WEBSITE_FROM_EMAIL must be a domain verified inside Resend.
BOOKING_EMAIL=booking@typhoon.band
WEBSITE_FROM_EMAIL=website@typhoon.band

# --- Site --------------------------------------------------------
# NEXT_PUBLIC_SITE_URL is also used by Supabase Auth as the configured
# Site URL — keep it in sync with Supabase → Authentication → URL
# Configuration. See docs/admin-setup.md.
NEXT_PUBLIC_SITE_URL=https://typhoon.band
