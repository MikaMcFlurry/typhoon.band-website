Read CLAUDE.md, docs/current-task.md, docs/00-project-source-of-truth.md and docs/02-claude-workflow.md.

Execute only the active phase listed in docs/current-task.md.

Open only the phase file needed for this task.
Do not read or follow old archived UI-fix prompts unless the active phase explicitly says so.

Do not change the approved frontend design unless the active phase explicitly requires it.

Work step by step.
Run npm run lint and npm run build.
Fix errors.
Push the completed phase as its own commit/branch.
Then summarize and stop.
